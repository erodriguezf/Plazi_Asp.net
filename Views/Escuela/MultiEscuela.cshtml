@model IEnumerable<Escuela>;

@{
    ViewData["Title"] = "Informacion sobre la Escuela ";
    Layout="_Simple";
}

<strong>Desde la vista parcial</strong>
<!--@foreach (var alumno in Model)
{
    <partial name="ObjetoSimple" model="alumno"/>
}-->
<!--table class="table table-dark table-hover">
    <thead>
        <th scope="col">Nombre</th>
         <th scope="col">Id</th>
    </thead>
    <tbody>
        @foreach (var alumno in Model)
        {
         <tr>
            <td>@alumno.Nombre</td>
             <td>@alumno.Id</td>
         </tr>
        }
    </tbody>
</table-->
<partial name="ListaObjetoEscuela">


